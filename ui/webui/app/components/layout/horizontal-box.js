import Ember from 'ember';
import StyleableMixin from '../../mixins/components/styleable';

export default Ember.Component.extend(StyleableMixin, {
  classNames: ['component-layout-horizontal-box']
});
