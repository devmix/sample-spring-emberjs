{{#each cells as |cell index|}}
  {{#if hasBlock}}
    {{yield (hash cell=(component 'e-table/grid-cell' model=cell index=index grid=grid)) this}}
  {{else}}
    {{component 'e-table/grid-cell' model=cell index=index grid=grid}}
  {{/if}}
{{else}}
  {{#if cells.isPending}}
    {{e-progress/loading}}
  {{else if cells.isRejected}}
    Fail
  {{else}}
    No data
  {{/if}}
{{/each}}