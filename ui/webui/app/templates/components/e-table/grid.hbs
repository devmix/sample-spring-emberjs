{{#if hasBlock}}
  {{yield (hash
            header=(component 'e-table/grid-header' attributesConfig=attributesConfig grid=this)
            cells=(component 'e-table/grid-cells' cells=cells grid=this)
          ) this}}
{{else}}
  {{component 'e-table/grid-header' attributesConfig=attributesConfig grid=this}}
  {{component 'e-table/grid-cells' cells=cells grid=this}}
{{/if}}
