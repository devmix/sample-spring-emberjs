{{#each component.table.columns.list as |column index|}}
  {{#if hasBlock}}
    {{yield (hash cell=(component 'e-table/cell' column=column row=this)) this}}
  {{else}}
    {{component 'e-table/cell' column=column row=this}}
  {{/if}}
{{/each}}
