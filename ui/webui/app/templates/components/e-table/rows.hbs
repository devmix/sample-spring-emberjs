{{#each items as |item index|}}
  {{#if hasBlock}}
    {{yield (hash row=(component 'e-table/row' model=item index=index grid=grid)) this}}
  {{else}}
    {{component 'e-table/row' model=item index=index grid=grid}}
  {{/if}}
{{/each}}
