<table class='ui celled striped compact table'>
  {{#if hasBlock}}
    {{yield (hash
              columns=(component 'e-table/columns' items=attributesConfig table=this)
              rows=(component 'e-table/rows' items=rows component=component table=this)
            ) this}}
  {{else}}
    {{component 'e-table/columns' items=attributesConfig table=this}}
    {{component 'e-table/rows' items=rows component=component table=this}}
  {{/if}}
</table>